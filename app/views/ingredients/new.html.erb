<img class="titlePic" src="http://thumbs.dreamstime.com/thumbimg_341/1228726954blJ3If.jpg">
<h1 class="title">Recipe Docs</h1><hr>

<%= link_to "Back to all recipes", recipes_path %>

<h2>Add Your Ingredients!</h2>

<%= form_for [@recipe, @ingredient] do |f| %>

  <% if @ingredient.errors.any? %>
    <div>
      <%= pluralize(@ingredient.errors.count, "error") %> prevented this ingedient from being submitted:
      <ul>
        <% @ingredient.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="box">
    <%= f.label "ingredients (one at a time)" %><br>
    <%= f.text_field :name %>
  </div>
  
  <div>
    <%= f.submit "Submit" %> 
  </div>
<% end %>